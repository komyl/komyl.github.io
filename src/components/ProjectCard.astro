---
import TechnologyIcon from './TechnologyIcon.astro';

export interface Props {
    name: string;
    status: 'active' | 'suspended' | 'finished';
    technologies?: string[];
    duration?: string;
}

const { name, status, technologies, duration } = Astro.props;

const validTechnologies = technologies?.filter(tech => tech && tech.trim() !== '') ?? [];
---
<div class="project-card">
    <div>
        <div class="card-header">
            <h3 class="project-name">{name}</h3>
            <div class="status-indicator" title={`Status: ${status}`}>
                <div class:list={['status-dot', `dot-${status}`]}></div>
            </div>
        </div>
        <div class="card-body">
            </div>
    </div>
    <div class="card-footer">
        <div class="technologies">
            {/* We now map over the clean array */
            validTechnologies.slice(0, 5).map(tech => (
                <TechnologyIcon name={tech} />
            ))}
        </div>
        {duration && <span class="duration">{duration}</span>}
    </div>
</div>

<style>
.project-card {
    background-color: var(--glass-background);
    backdrop-filter: blur(20px);
    -webkit-backdrop-filter: blur(20px);
    border: 1px solid var(--glass-border);
    border-radius: var(--border-radius-large);
    padding: calc(var(--spacing-unit) * 3);
    display: flex;
    flex-direction: column;
    justify-content: space-between;
    height: 100%;
    transition: transform 0.25s ease-out, box-shadow 0.25s ease-out;
}
.project-card:hover {
    transform: scale(1.015);
    box-shadow: 0 12px 25px rgba(0, 0, 0, 0.15);
}
.card-header {
    display: flex;
    justify-content: space-between;
    align-items: flex-start;
    margin-bottom: calc(var(--spacing-unit) * 2);
}
.project-name {
    font-size: 1.5rem;
    font-weight: 600;
    color: var(--color-text);
    margin-right: 1rem;
}
.status-indicator {
    flex-shrink: 0;
    padding-top: 0.5rem;
}
.status-dot {
    width: 10px;
    height: 10px;
    border-radius: 50%;
    animation: blink 1.8s infinite ease-in-out;
}
.dot-active { background-color: #34c759; }
.dot-suspended { background-color: #ffcc00; }
.dot-finished { background-color: #8e8e93; }

@keyframes blink {
    0%, 100% { opacity: 1; transform: scale(1); }
    50% { opacity: 0.4; transform: scale(0.85); }
}
.card-body {
    flex-grow: 1;
    min-height: calc(var(--spacing-unit) * 4);
}
.card-footer {
    display: flex;
    justify-content: space-between;
    align-items: flex-end;
    gap: var(--spacing-unit);
}
.technologies {
    display: flex;
    flex-wrap: wrap;
    gap: var(--spacing-unit);
}
.duration {
    font-size: 0.9rem;
    color: var(--color-text-muted);
    white-space: nowrap;
}
</style>